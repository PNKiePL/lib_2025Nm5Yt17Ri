#pragma once


#include<iostream>
#include<bitset>
#include<windows.h>
#include<cstdio>
#include<conio.h>
#include<stdio.h>
#include<fstream>
#include <string>

#include "E:\lib\文件读写.cpp"

using namespace std;

fstream 键盘键位读取文档;
fstream 键盘键位写入文档;
string 输入, 第一个, 第三个, 第二个;

//void 长字符(const string a)
//{
//	if (a[0] != '-')if (!isspace(a[0])) {
//		//cout << "color(7); if (GetKeyState(" << b << ") < 0) color(177); prautof(" << b << ");  color(7); ";
//		if (a[1]) {
//			cout << "长字符(" << a << "," << "冒号" << a << "冒号" << "); "; //cout << "显示文字('" << b << "'); ";
// //else cout << "显示文字(" << b << ");";
//			cout << "//" << 个数; 换行 += 1;
//			cout << endl;
//		}
//		if (换行 == 9) { cout << "换行换行换行换行" << endl; 换行 = 0; }
//		个数 -= 1;
//	}
//}
